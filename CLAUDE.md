# CLAUDE.md

See [`AGENTS.md`](AGENTS.md). It is the canonical instruction file for this
repository and applies to every agent, not just Claude.

Per-app rules live in `apps/*/AGENTS.md`. Reasoning lives in
[`docs/`](docs/README.md).
