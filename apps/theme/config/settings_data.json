/*
 * ------------------------------------------------------------
 * IMPORTANT: The contents of this file are auto-generated.
 *
 * This file may be updated by the Shopify admin theme editor
 * or related systems. Please exercise caution as any changes
 * made to this file may be overwritten.
 * ------------------------------------------------------------
 */
{
  "current": {
    "settings": {
      /*
       * Klaviyo onsite credentials for the footer email capture form.
       *
       * Committed rather than set in the theme editor, for the same reason the
       * app embed below is: `.shopifyignore` does not exclude this file, so a
       * `shopify theme push` overwrites whatever the editor set.
       *
       * ⚠️ Both values are public by design. The onsite key is scoped to
       * writing events and subscriptions for one account and is *meant* to
       * ship in client code — it is the same key `NEXT_PUBLIC_KLAVIYO_PUBLIC_KEY`
       * carries on apps/web. A list id names a destination, not a permission.
       *
       * A Klaviyo **private** key must never appear here, or anywhere in this
       * repository — see the root AGENTS.md.
       */
      "klaviyo_public_key": "X2g4U5",
      "klaviyo_list_id": "UMg9yt"
    },

    /*
     * App embeds. Committed rather than enabled through the theme editor,
     * because `.shopifyignore` does not exclude this file — so `shopify theme
     * push` overwrites whatever the editor set, and an embed enabled there
     * would silently disappear on the next push.
     *
     * Same reasoning as the committed assets/tokens.css: state the theme
     * depends on lives in the repository, not in the admin.
     *
     * The UUID identifies Klaviyo's theme app extension, not this store, so
     * this block is portable to any store with the app installed. The outer
     * key is an arbitrary unique id.
     *
     * Note this only covers THIS theme. App embeds are per-theme, and neither
     * apps/web nor apps/mobile has an embed mechanism at all — see
     * docs/integration-klaviyo.md.
     */
    "blocks": {
      "855628211100114053": {
        "type": "shopify://apps/klaviyo-email-marketing-sms/blocks/klaviyo-onsite-embed/2632fe16-c075-4321-a88b-50b567f42507",
        "disabled": false,
        "settings": {}
      }
    }
  }
}
